# 首版本验收记录

日期：2026-09-23。基准：`output/prototypes/v1/01-dashboard.png` 至 `04-validation.png`，即用户已确认的四张原型。

## 原型对照

使用 Codex 内置浏览器运行真实本地页面，桌面 1536×1024、手机 390×844；截图保存在 `output/assistant-verification/`。逐张查看原型与实际截图，未将原型图片作为页面背景。

| 对照点 | 实现与验证 |
|---|---|
| 页面结构 | 工作台四指标、行情与情绪双栏、策略横条；选股表格与详情；个股行情/条件/财务；验证分类，保持原型顺序 |
| 导航与图标 | 236px 深蓝侧栏，线性 SVG 图标；手机改底部导航，保留核心入口 |
| 色彩与容器 | 冷白背景、白面板、蓝色交互、涨红跌绿、琥珀风险提示，与原型一致 |
| 字体与密度 | 系统中文字体，30px 页标题、14px 正文、12px 辅助信息；真实长文本适度换行 |
| 表格与响应式 | 修复选股网格最小宽度导致的整页溢出；390px 下表格在自身容器横向滚动，页面无横向溢出 |
| 图表与布局 | 原生 SVG 日K、成交量与营收图；压缩工作台图表和底部行间距，成交量与价格横轴对齐 |
| 内容可信度 | 原型演示数字替换成公开行情及归档；显示行情与分析各自时间，缺失数据明确标注 |
| 交互 | 导航、搜索、状态筛选、选中股票、个股详情、验证记录及模拟验证切换通过浏览器检查 |

截图：`dashboard-desktop.png`、`selection-desktop.png`、`stock-desktop.png`、`validation-desktop.png`；手机证据 `dashboard-mobile.png`、`selection-mobile.png`、`validation-mobile.png`。

有意差异：指数使用可核验日K而非原型示意分时；归档采样不冒充逐笔分时。实际未产生有效推荐/模拟交易时使用空状态，不填入示意盈利。财务按报告期累计口径显示；公告正文尚未接入。手机面板顺序排列，不压缩双栏。没有新增 React/Vite 依赖，以标准库服务与原生 ES modules 实现相同界面结构。

## 工程与业务验证

- `/Users/habitat/miniforge3/bin/python3 -m pytest -q`：151 passed，15 subtests passed。
- `python -m compileall -q assistant`：通过。
- `node --check assistant/static/app.js`、`pages.js`、`ui.js`：通过。
- `git diff --check`：通过。
- 浏览器控制台检查无 error/warn；真实行情、日K、8期财报读取成功。
- 独立审查已完成，已解决发现的问题，无剩余审查项。

测试包括：不可变推荐与去重、历史推荐保留、过期/失败模型关闭买入入口、滚动池移除后取消权限、行情网络读取期间权限变化、跨来源请求拒绝、模拟资金预留、到期释放、T+1，以及排板/回封快照不得假定成交。模拟用临时测试数据验证，未对用户账本创建交易。

## 交付边界

代码：`assistant/`、四个 `tests/test_assistant_*.py`，根 README 启动说明和忽略数据库规则。没有修改现有监控核心、交易计划、凭证或真实持仓；没有提交或推送本轮代码。

本版本支持推荐归档、推荐后走势跟踪与独立模拟账本。完整历史收益回测、收益优化闭环、公告正文、国金实盘接入尚未完成。模拟费用为明确的简化假设，不声称完整税费/滑点或真实可成交性。公开数据源可能限流。未运行真实券商端到端测试，也未执行实盘交易。

技术研究辅助，不构成投资建议。

## 策略展示扩展（2026-09-23）

新增“我的策略”导航与工作台入口；沿用已确认的侧栏、色彩、字体和面板，不改原页面结构。只读策略目录与版本独立于交易计划，当前短线框架/价值研究模板清晰区分；展示七类研究条件及最近归档风险与缺口。切换模板仅影响页面选择，不产生执行或账本写入。

验证：全量 157 passed / 15 subtests；最终说明文字调整后定向策略和HTTP测试 11 passed；Python compileall、JS语法检查、git diff --check 通过。浏览器 1536×1024 截图对照既有风格，390×844 模板切换成功，整页宽度390无横向溢出，控制台无错误。截图 strategies-desktop.png / strategies-mobile.png。实际策略引擎切换未开放，价值模板无收益回测或选股执行能力。

策略页独立审查发现默认弱转强参数可能与用户计划不同，已明确标注为仓库默认阈值，并说明每股最新计划可覆盖、页面说明不是实时参数。修正后策略测试5项通过；无剩余已知审查问题。

## 选股策略来源与变化对比

新增来源标签（观察池/个股/推荐验证）、只读历史比较API与前后归档选择；保持旧推荐账本格式。新增报告策略指纹写入及滚动研究规范，同步已安装的对应脚本与规范；未修改计划、模型配置或风控判断。版本随原计划或规则变化；历史缺失策略信息不回填。浏览器验证来源未标注、前后选择、失败快照不可比较以及390px无溢出。收益回测及策略执行切换未扩展。

本轮最终验证：`/Users/habitat/miniforge3/bin/python3 -m pytest -q` 为 173 passed / 15 subtests passed；Python compileall、app.js/pages.js 的 node --check、git diff --check 通过。独立审查发现并修复模型子集可能伪报移出的问题：只有顶层完整候选列表及有效时间可比较，局部 assessment/decision 不作为完整池。复核无剩余 P0/P1。桌面/窄屏交互及控制台检查通过；未执行实盘、未跑新交易轮次。下一份真实 rolling 归档的来源指纹仍需在自动化运行后验收，旧归档保持来源未标注。
